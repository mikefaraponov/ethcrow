export default function NetworkStatus({getState}) {
  return (next) => (action) => {
    const {type, payload} = action;
    if (type === SIGN_UP) {
      case :
    }
    return next(action);
  };
}
